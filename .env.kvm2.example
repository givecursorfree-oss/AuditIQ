# 8 GB VPS env (no Typesense / Tika by default). Copy on the server:
#   cp .env.kvm2.example .env.kvm2
# Then: docker compose -f docker-compose.kvm2.yml --env-file .env.kvm2 up -d --build
#
# Claims work without OCR. Optional receipt OCR (Paddle) needs ~1.5 GB extra:
#   PADDLE_OCR_URL=http://paddleocr:8090
#   docker compose -f docker-compose.kvm2.yml --env-file .env.kvm2 --profile ocr up -d --build

MYSQL_ROOT_PASSWORD=CHANGE_ME_long_random_mysql_password
JWT_SECRET=CHANGE_ME_at_least_32_characters_random_string
VAULT_ENCRYPTION_KEY=CHANGE_ME_another_32_char_minimum_secret

# Use your real domains:
CLIENT_URL=https://auditiq.mkdandeker.com
DOMAIN=auditiq.mkdandeker.com
# API on parent domain (NOT api.auditiq… and NOT app.auditiq…)
API_DOMAIN=api.mkdandeker.com

ALLOW_STAFF_REGISTRATION=false
SKIP_EMAIL_VERIFICATION=false
MAX_FILE_SIZE_MB=25

# Empty = OCR disabled (claims still submit; managers just won't see OCR amount)
PADDLE_OCR_URL=

# SMTP delivery (required for email)
SMTP_HOST=smtp.your-provider.com
SMTP_PORT=587
SMTP_USER=notifications@your-domain.com
SMTP_PASSWORD=change-me-use-an-app-password
SMTP_FROM=Your CA Firm <notifications@your-domain.com>
