/** @deprecated Alerts render in top Dynamic Island — type kept for chat context */
export type TeamsToastPayload = {
  id: string;
  roomId: string;
  senderName: string;
  senderInitials: string;
  roomName: string;
  preview: string;
  timeLabel: string;
};
