# Hostinger KVM 2 (2 vCPU / 8 GB) — production compose
# Usage: docker compose -f docker-compose.kvm2.yml --env-file .env.kvm2 up -d --build
#
# Includes Tika + PaddleOCR (receipt/document text). Typesense still off (MySQL search).
# mem_limit is required — deploy.resources.limits is ignored outside Swarm and Paddle
# can OOM-kill the Node API (nginx then returns 502).

services:
  db:
    image: mysql:8.0
    restart: unless-stopped
    environment:
      MYSQL_DATABASE: auditiq
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:?MYSQL_ROOT_PASSWORD must be set}
    command:
      - --innodb-buffer-pool-size=512M
      - --max-connections=100
    volumes:
      - mysql_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s
    mem_limit: 1280m

  tika:
    image: apache/tika:3.3.0.0-full
    restart: unless-stopped
    mem_limit: 640m

  paddleocr:
    build: ./services/paddleocr-sidecar
    restart: unless-stopped
    mem_limit: 1280m

  server:
    build: ./server
    restart: unless-stopped
    # API must not wait on OCR sidecars — they can be slow/OOM; OCR fails open at runtime
    depends_on:
      db:
        condition: service_healthy
    environment:
      DATABASE_URL: mysql://root:${MYSQL_ROOT_PASSWORD}@db:3306/auditiq
      JWT_SECRET: ${JWT_SECRET:?JWT_SECRET must be set}
      NODE_ENV: production
      PORT: 3001
      CLIENT_URL: ${CLIENT_URL:?CLIENT_URL must be set}
      LOG_LEVEL: info
      MAX_FILE_SIZE_MB: ${MAX_FILE_SIZE_MB:-25}
      VAULT_ENCRYPTION_KEY: ${VAULT_ENCRYPTION_KEY:?VAULT_ENCRYPTION_KEY must be set}
      SEMANTIC_SEARCH_ENABLED: "false"
      TYPESENSE_HOST: http://127.0.0.1:8108
      TYPESENSE_API_KEY: unused-on-kvm2
      TIKA_URL: http://tika:9998
      PADDLE_OCR_URL: http://paddleocr:8090
      ALLOW_STAFF_REGISTRATION: ${ALLOW_STAFF_REGISTRATION:-false}
      SKIP_EMAIL_VERIFICATION: ${SKIP_EMAIL_VERIFICATION:-true}
      SMTP_HOST: ${SMTP_HOST:-}
      SMTP_PORT: ${SMTP_PORT:-587}
      SMTP_USER: ${SMTP_USER:-}
      SMTP_PASSWORD: ${SMTP_PASSWORD:-}
      SMTP_FROM: "${SMTP_FROM:-AuditIQ <no-reply@auditiq.local>}"
      GOOGLE_CLIENT_ID: ${GOOGLE_CLIENT_ID:-}
      GOOGLE_CLIENT_SECRET: ${GOOGLE_CLIENT_SECRET:-}
      GOOGLE_REDIRECT_URI: ${GOOGLE_REDIRECT_URI:-}
    volumes:
      - server_logs:/app/logs
      - document_uploads:/app/uploads
    healthcheck:
      test:
        [
          "CMD",
          "node",
          "-e",
          "fetch('http://127.0.0.1:3001/api/health').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))",
        ]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 90s
    mem_limit: 1024m

  client:
    build: ./client
    restart: unless-stopped
    depends_on:
      server:
        condition: service_started
    ports:
      - "80:80"
      - "443:443"
    environment:
      DOMAIN: ${DOMAIN:-}
      API_DOMAIN: ${API_DOMAIN:-}
    volumes:
      - certbot_conf:/etc/letsencrypt:ro
      - certbot_www:/var/www/certbot
    mem_limit: 192m

  certbot:
    image: certbot/certbot
    volumes:
      - certbot_conf:/etc/letsencrypt
      - certbot_www:/var/www/certbot
    profiles:
      - ssl

volumes:
  mysql_data:
  server_logs:
  document_uploads:
  certbot_conf:
  certbot_www:
